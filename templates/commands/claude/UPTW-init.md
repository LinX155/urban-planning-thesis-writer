---
name: UPTW Init
description: Initialize a thesis workspace for UPTW
category: Workflow
tags: [uptw, thesis, init]
---

Use the `$uptw-init` skill for this request.

Treat everything after `/UPTW-init` as inline initialization context. Initialize the thesis workspace, verify the state tree, and return only the state path plus the next suggested command.
