interface:
  display_name: "UPTW Plan"
  short_description: "锁定论文证据边界、章节图和写作规格"
  default_prompt: "Use $uptw-plan to lock the thesis evidence boundary, chapter graph, and section briefs before drafting."

policy:
  allow_implicit_invocation: false
